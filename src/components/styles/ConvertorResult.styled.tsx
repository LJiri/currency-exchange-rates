import styled from "styled-components";

export const ConvertorResult = styled.div`
  font-size: 40px;
  text-align: center;
  background: var(--color-white);
  border-radius: var(--border-radius);
  padding: 40px;

  div:not(:last-child) {
    margin-bottom: 30px;
  }
`;
